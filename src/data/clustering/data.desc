聚类数据描述：
1. kmeans.txt：kmeans算法训练数据，数据有k个属性，这些属性值是数值类型（且只能是数值类型），kmeans训练数据没有
    目标列,聚类结果为n个聚类中心（即n个类）；

2. gmm.txt：GMM算法训练数据，数据有k个属性，这些属性值是数值类型（且只能是数值类型），数据没有目标列。

3. pic.txt：PIC算法训练数据，模型输入数据格式必须为：(srcId, dstId, similarity)，且类型对应为
    (Long, Long, Double)这样的一个三元组。注意：由于算法可以进行列选择，因此原始数据不必是(srcId, dstId, similarity)格式。
    输入数据中，数据对（srcId,dstId）不管先后顺序如何只能出现至多一次（即是无向图，即两顶点边权重是唯一的）
    如果输入数据中不含这个数据对（不考虑顺序），则这两个数据对的相似度为0（没有这条边）。